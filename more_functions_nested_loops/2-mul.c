#include <stdio.h>
#include "main.h"
/**
 * mul - Multiplies two integers
 * @a: first integer in equation
 * @b: second integer in equation
 *
 * Return: Product of two integers
 */
int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
