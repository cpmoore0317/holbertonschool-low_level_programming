#include <stdio.h>

/**
 * main - Prints its name with a newline
 * @argc: Number of arguments typed
 * @argv: Array pointing to arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
